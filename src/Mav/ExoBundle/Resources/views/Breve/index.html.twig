{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Breve list</h1>
    
    <ul>
        <li>
            <a href="{{ path('admin_breve_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    
    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Slug</th>
                <th>Content</th>
                <th>Status</th>
                <th>Cdate</th>
                <th>Udate</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td><a href="{{ path('admin_breve_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.slug }}</td>
                <td>{{ entity.content }}</td>
                <td>{{ entity.status }}</td>
                <td>{% if entity.cdate %}{{ entity.cdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.udate %}{{ entity.udate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_breve_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_breve_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    
    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

        
    {% endblock %}
